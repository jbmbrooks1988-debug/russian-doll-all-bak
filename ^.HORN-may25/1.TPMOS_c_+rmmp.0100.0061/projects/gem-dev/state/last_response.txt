I've created `another_test_file.txt` and then listed the directory contents. It now shows `test_file.txt`, `dir.txt`, and `another_test_file.txt`. It seems the `list_dir` command is working correctly now!