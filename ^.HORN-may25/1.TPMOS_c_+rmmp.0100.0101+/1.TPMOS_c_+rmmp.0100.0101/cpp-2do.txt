# cpp-llm Technical Refactor & Tool Integration Plan

## Phase 1: API Caller Refactor (Match --notools)
- [ ] **Manager Logic (`cpp-llm_manager.c`):**
    - [ ] Import `<ifaddrs.h>` and `<netdb.h>` for IP resolution.
    - [ ] Implement `resolve_my_ip()` to detect local network identity.
    - [ ] Update `g_api_url` default to `http://10.0.0.144:8080` (Aida Mac).
    - [ ] Implement `g_yolo_mode` and permission states (`g_permission_pending`, etc.).
    - [ ] **Refactor `start_ai_query`:**
        - [ ] Switch from `gemini_payload_builder` to `text_to_llama3` + `json_escaper` pipeline.
        - [ ] Use `connect_op` instead of direct `curl` fork for stability and logging.
        - [ ] Force OpenAI-compatible `/v1/chat/completions` payload structure.
    - [ ] **Refactor `check_ai_status`:**
        - [ ] Implement multi-tier JSON parsing (Llama.cpp vs Ollama structures).
        - [ ] Handle `choices[0].message.content` for standard API and raw `content` for local.
        - [ ] Implement the "ANSI Red" y/n permission prompt in `g_sys_msg`.

## Phase 2: Missing Ops Implementation
- [ ] Port `ops/src/connect_op.c` from `--notools` to `cpp-llm`.
- [ ] Port `ops/src/text_to_llama3.c` from `--notools` to `cpp-llm`.
- [ ] Port `ops/src/json_escaper.c` from `--notools` to `cpp-llm`.
- [ ] Update `build.sh` to include these new ops.

## Phase 3: "Naked" Tool Call Reverse Engineering
- [ ] **Diagnostic:** Run `test_apis.sh` against the local 8080 endpoint to capture raw tool call output.
- [ ] **Problem:** Local Llama.cpp doesn't support native `tool_calls` in the JSON schema like Gemini.
- [ ] **Solution:** Implement a "Naked Parser" that detects JSON blocks within the `content` string if no native `tool_calls` are found.
- [ ] **Implementation:**
    - [ ] Update `check_ai_status` to scan `content_json` for `{"tool": "..."}` patterns if `tool_name` is null.
    - [ ] Map the "Naked" JSON fields (e.g., `tool` -> `g_pending_tool_name`, `args` -> `g_pending_args_json`).

## Phase 4: Validation & YOLO Mode
- [ ] Verify `config/yolo.flag` bypasses permission prompts.
- [ ] Test the full loop with a `read_file` tool call generated by the local model.
- [ ] Ensure `pieces/os/proc_list.txt` is correctly updated via the refactored `log_pid`.
