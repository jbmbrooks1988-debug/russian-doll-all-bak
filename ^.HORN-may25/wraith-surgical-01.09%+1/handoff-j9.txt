WRAITH MOUSE HANDOFF (J9)
Date: 2026-06-09
Status: HANDOFF FOR NEXT AGENT

1. What is working now
----------------------
- The restored baseline in `1.TPMOS_c_+rmmp.0102.0020` has Enter/button navigation working again.
- Wraith taskbar navigation is still present.
- There is some mouse plumbing already in the tree:
  - the parser can write mouse movement events into the Wraith semantic inbox
  - the manager can consume `MOUSE_MOVE` commands
  - the layout already contains a Wraith debug area
- The restored baseline is the one to use as the starting point. Do not continue from the experimental parser-hit-test branch.

2. What was tried and why it was rolled back
-------------------------------------------
- I attempted parser-side hit-testing and Wraith click-target debugging.
- That branch introduced instability:
  - Enter/button activation became unreliable
  - mouse hit testing stayed mismatched with the visible Wraith taskbar target
- The tree was restored to the last known-good baseline before handing off.

3. Current architecture intent
------------------------------
- Keep Wraith on a single semantic inbox:
  - `projects/wraith-alpha/session/history.txt`
- Keep raw mouse capture separate from project semantics:
  - `pieces/keyboard/history.txt` remains the raw audit/input ledger
- Preserve the manager as the owner of Wraith desktop state:
  - `wraith-alpha_manager.c` updates `alpha_state.txt` / `desktop_ui_state.txt`
  - manager pulses `pieces/display/frame_changed.txt`
- Preserve parser/layout responsibilities:
  - `alpha-shell.chtpm` renders the desktop and debug block
  - parser should only bridge cleanly to the manager, not create a second click path

4. Known risks / open problems
------------------------------
- Mouse click hit-testing is still the main unresolved problem.
- The strict row/column assumptions used in the experimental branch were too brittle.
- Do not reintroduce manager-side hardcoded hot-zones as the long-term fix.
- Do not fake Enter/keyboard events from mouse clicks. That caused ambiguity and double-path risk.

5. Recommended next order of work
---------------------------------
1. Reconfirm the restored baseline still compiles cleanly.
2. Stop frame spam first: mouse movement must not dirty frames.
3. Only click transitions should mark the frame dirty.
4. Reconfirm Wraith Enter/button launch after the redraw path is quiet.
5. Add only read-only mouse debug first if you need more visibility.
6. If you reattempt click support, do it in the parser with a clear coordinate model and a small, isolated change.
7. Keep the change set narrow and verify each slice with a human retest before moving on.

6. Files to inspect first
-------------------------
- `1.TPMOS_c_+rmmp.0102.0020/projects/wraith-alpha/manager/wraith-alpha_manager.c`
- `1.TPMOS_c_+rmmp.0102.0020/projects/wraith-alpha/layouts/alpha-shell.chtpm`
- `1.TPMOS_c_+rmmp.0102.0020/pieces/chtpm/plugins/chtpm_parser.c`
- `step-1-temporary.txt`
- `wraith-mouse-j9.txt`

7. Notes for the next agent
---------------------------
- Treat `step-1-temporary.txt` as the active progress ledger.
- Treat this handoff as the “do not lose the working baseline” note.
- Start from the restored tree, not from the experimental parser-hit-test attempt.
- The task is still mouse integration, but the first goal is stability and clean separation of responsibilities.
