---------]


load is stuck. seems it would be easy fix. 
esp if things were more modular. i spent 2 days brute forcing a fix. 

u need to approach from a diff angle. skip it. 


maybe get "play game" to work at least? 

make a default game load at start? etc. how does fuzzop load? wussup? 
-------------
🚿️
heres the fix.

have it make "default-0000"

then have it auto load that at start. 

then in new context. 
have it factor out load 2 op. 

then use that op in file browser load. voila🚿️
