## 1. Objectives
- [x] **<img> Texture Support:** Functional texture loading via `stb_image` with corrected coordinates.
- [x] **<canvas> 3D Context:** Perspective-based 3D emoji map rendering (Piececraft-3D style).
- [x] **Scrollable Emoji Picker:** 2x2 FreeType-powered grid with thumb scroll.
- [x] **Layout Polish:** Options menu repositioned; vertical slider made horizontal.

## 2. Technical Details
- **FreeType Integration:** Linked `NotoColorEmoji.ttf` to the GL-OS renderer for high-fidelity emoji textures.
- **Hierarchical Rendering:** Every element (including 3D sprites on the canvas map) respects the matrix stack and scissor boundaries.
- **Menu Activation:** Clicking a `<menu>` tag sets `active_node_index`, resetting navigation context for sub-options.

## 3. Handoff Notes (Next Steps)
1.  **Picker Interaction:** Map mouse clicks/joystick buttons to `picker_scroll` and `active_emoji` selection.
2.  **Emoji Placement:** Implement `PLACE` command logic to add selected emojis from picker to the 3D `<canvas>` map.
3.  **Audit Persistence:** Ensure the hierarchical tree is reflected in `audit_frame.txt` for perfect terminal mirroring.

---
*"The UI is the document. The document is the world."*
