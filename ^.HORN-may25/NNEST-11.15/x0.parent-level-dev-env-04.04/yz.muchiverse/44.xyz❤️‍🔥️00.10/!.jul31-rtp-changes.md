# 🗓️ July 31 — rtp-xyz (and rpg-xyz) changes, session close-out

Everything touched today across `300.rtp-xyz` and, where noted, its
sibling `300.rpg-xyz`. `101.mutaclsym🧟‍♂️️+18.01` itself was kept as an
**untouched reference** the whole session — every real code edit
landed only in the two clones, per your own explicit rule. Ordered
roughly as it happened.

---

## 🧩 1. Phase 2 module-split rollout — finished, house-wide

- ✅ `0.user-pal👤️/00.login-signup` — index port + debug-spam removal.
- ✅ `300.rpg-xyz` / `300.rtp-xyz` (mutaclysm's own copies) — per-screen
  `.pal` module split + index port.
- ✅ `0.user-pal👤️/01.avatar-creation👤️` — full 6-screen module split,
  verified via `test-harn-same/scenarios/demo_menu_fx.sh` (20/20
  assertions passed).
- 📄 Real per-project `phase2-module-split-report.txt` written for
  each of the above, plus the earlier-phase projects.

## 🎨 2. De-theming rtp-xyz + rpg-xyz — generic RPG-Maker-style content

- 🧟➡️🟢 `zombie`/`zombie_child` → `slime`/`slime_pup` in both projects'
  monster registries, `ops/tick_monsters.c`, `ops/move_player.c`, and
  every live `world_01_template` monster instance — same stats, new
  identity.
- 🔍 Discovered terrain/furniture/items were **already generic** — a
  narrower real scope than assumed going in.
- 📄 `de-theme-report-j31.txt` written for both projects.
- ✅ Bonus find: the action-bar menu was **already** dynamic
  (`fo-menu-sys`-driven), not hardcoded — one less thing to fix.

## 💾 3. Real save/load for rtp-xyz — `manager/rtp_manager.c` (new file)

Modeled on TPMOS's real `slop-ed-dev_manager.c`. New Save/Load screen
(`pieces/chtpm/layouts/project_menu.chtpm`), wired into `game.chtpm`.

Five real bugs found and fixed while building it:
- 🚫 Wrong `onClick` prefix (`LOAD:`/`SAVE_NEW` silently dropped —
  house **PITFALL #99**) → fixed to `SET_LOAD:`/`SET_SAVE_NEW`.
- 📁 Wrong relay-file path assumed → corrected to the real
  `interact_relay.txt` path `game.chtpm` already uses.
- 📍 Wrong `gui_state.txt` path, twice, before landing on the real
  default path.
- 💥 `trigger_render()` was clobbering the save list by shelling out to
  mutaclysm's own game-specific frame composer — removed entirely.
- 🆕 **New house-wide PITFALL 68 discovered and documented**: CHTPM's
  frame composer only re-renders `${var}` content when
  `piece_methods`/`active_target_id`/`input_text` change — any other
  custom variable freezes after first render. Fixed by publishing the
  save list *as* `piece_methods`.

## 🔥 4. Serious bug caught and fixed: save/load was deleting the registry

- `do_load()`'s original code ran an unguarded `rm -rf
  pieces/registry` **before** confirming the save being loaded
  actually had one — a silent `cp` failure at save time meant a real
  save could permanently wipe the live registry with zero error
  shown.
- 🚑 Recovered rtp-xyz's `pieces/registry/` from rpg-xyz's own intact,
  identically de-themed copy.
- 🛡️ Rewrote `do_save_new()`/`do_load()` with per-step exit-code
  checks and a real safety gate: nothing destructive runs until the
  save's own source content is confirmed non-empty.
- 📄 Full writeup: `300.rtp-xyz/local-pitfalls-j31.txt` (Pitfall B).

## 🖥️ 5. CPU / stray-process safety hardening

- ➕ `EMERGENCY_KILL.sh` — added `rtp_manager` to its kill list (it was
  missing the new daemon entirely).
- ➕ `300.rtp-xyz/button.sh`'s own `kill` action — added a
  `pkill -9 -f "manager/+x/"` line (previously only covered `ops/+x/`
  and `system/` daemons).

## 🎥 6. Camera regression — root-caused and fixed in all 3 projects

- 🔎 Root cause: **not a code bug** — confirmed byte-identical
  camera/movement code between the current build and an earlier
  session snapshot. The real cause was my own repeated
  `world_01_template` resets during testing silently discarding your
  real, hand-positioned camera state (no error, just quiet data loss).
- ✅ Restored real values (`cam_yaw=180°`, `cam_pitch=6°`,
  `cam_z_level=2`, `hero_z=14`, `facing=1002`, `camera_mode=2`,
  `render_mode=1`) into **mutaclysm, rtp-xyz, and rpg-xyz alike**, per
  your own explicit call.
- 🌱 Also baked into `world_01_template` in all three, so new games
  start with this view from now on, not the old hardcoded default.
- 📄 `local-pitfalls-j31.txt` written in all three projects (Pitfall
  A).

## 🌈 7. 3D emoji/texture rendering — gap closed in rtp-xyz + rpg-xyz

- 🔍 Root cause: both projects had an **older** `ops/compose_rgb_
  frame.c` lacking `ensure_emoji_asset_ready()` entirely — real gap
  from an incomplete earlier port, confirmed via diff against
  mutaclysm's current version.
- 📦 Ported the real files from mutaclysm: `compose_rgb_frame.c`,
  `emoji_gen_atlas.c`, `emoji_xtract.c`, `ops/lib/stb_image.h`.
- 🔧 Updated `scripts/build.sh` in both projects (FreeType/stb flags).
- ✅ Live-verified: a real slime texture generated on-demand during
  actual gameplay (180/256 non-transparent pixels).

## 🕹️ 8. Hero possession — confirmed working, not a bug

- Real control model ground-truthed: **arrow keys move the free
  Xlector cursor**, Enter on the target's own tile possesses it, then
  arrows move that entity. WASD is camera-only, never movement. Live
  test confirmed this already worked — the earlier report was a
  control-model misunderstanding, not a defect.

## 📘 9. Architecture design docs — written and confirmed

- `300.rtp-xyz/dox/rtp-xyz-architecture.md` and `300.rpg-xyz/dox/
  rpg-xyz-architecture.md` — full breakdown of event system, database
  editor, plugin system, GUI architecture differences between the two
  variants, save/load design, testing discipline, de-theming, and
  future-direction appendices.

## 🖼️ 10. GUI redesign — 3 rounds of correction, then implemented

This was the big one, and it went through real back-and-forth before
any code landed:

- **Round 1**: corrected my assumption that a Title Screen was the
  default boot target — real RPG Maker MZ separates **Editor** (dev
  tool) from **Player** (reached only via Playtest).
- **Round 2**: you showed real reference frames (wsr-pal's wrap-around
  menu, slop-ed-dev's map/glyph-picker layout) and asked for a written
  guide + mockup before building — `user-rtp-gui-guide.txt` and
  `claude-rtp-gui-view.txt` written.
- **Round 3**: direct correction — mutaclysm's survival actions
  (pickup/drop/eat/craft/examine/help) don't belong on the
  editor's default screen at all; pointed me at the real TPMOS
  `slop-ed-dev` project and the real RPG Maker MV/MZ toolbar list to
  copy from.
- 🛑 You then held the gate explicitly: **no code until the design
  docs were drastically rewritten and shown correctly.** Rewrote
  `rtp-gui-design.md` (v3) and `claude-rtp-gui-view.txt` (v2) as the
  final, consistent design — merged single Editor screen, real
  `<row>`/`<scroller>` primitives, RPG-Maker toolbar with honest
  "coming soon" placeholders for unbuilt features.
- ✅ You approved it — **implementation landed today**:
  - 🆕 `pieces/chtpm/layouts/editor_home.chtpm` — the new default
    boot screen: title/project row, real map render (untouched),
    "Control Map" (Xlector free-roam), real MAPS/GLYPHS data rows,
    RPG-Maker toolbar row (New/Open/Save/Cut/Copy/Paste/Delete, Map/
    Event Mode, Layer 1-3, Database, Plugins, Resources, CharGen —
    real focusable rows, honestly inert until their own milestones
    are built), and a real "Playtest" button into `game.chtpm`.
  - ✏️ `game.chtpm` — title rebranded from "MUTACLSXM" to
    "RTP-XYZ PLAYTEST", the leftover "Info (href test)" button
    replaced with a real "Stop Playtest" link back to
    `editor_home.chtpm`. Pickup/drop/eat/craft/examine/help/save all
    stay exactly as they were — this is Player-mode content now,
    correctly scoped off the Editor's own screen.
  - 🔧 `button.sh` and `system/orchestrator.c` — default boot target
    switched from `game.chtpm` to `editor_home.chtpm` (both the
    `--pal` path and the plain-C-mode fallback path).
  - ⚠️ **Not yet done tonight**: a live key-injection smoke test of
    `editor_home.chtpm` was queued but not completed before session
    close — first thing to verify next session before trusting the
    new default boot flow end-to-end. `title.chtpm` (New Game/
    Continue) also isn't built yet — Playtest currently jumps straight
    into `game.chtpm`.

## 📄 11. User-facing docs written today

- `rm-xyz-prog-report-j31.txt` — hands-on testing steps for
  everything up through the save/load work.
- `300.rtp-xyz/dox/rtp-gui-design.md`, `user-rtp-gui-guide.txt`,
  `claude-rtp-gui-view.txt` — the GUI design trail (§10 above).
- `#.haiku+/!.xyzos-pitfalls+1.txt` — house-wide **PITFALL 68** added.
- Local `local-pitfalls-j31.txt` in mutaclysm, rtp-xyz, and rpg-xyz.

---

## ▶️ Next session — real starting points

1. Live-test `editor_home.chtpm` (key injection, confirm it boots,
   renders, and Control Map/Playtest both work).
2. Build `title.chtpm` (New Game/Continue/Options/Back to Editor).
3. Apply the wrap-around method-list fix to `game.chtpm` (scoped edit
   to rtp-xyz's own local `load_dynamic_methods()` — planned, not yet
   done).
4. Carry the same GUI shape over to `300.rpg-xyz` (its own widget-
   based architecture, not yet designed in this much detail).
5. `300.rpg-xyz`'s own save/load widget (`&.widgits/project-menu/` —
   task #31, still pending).
6. Resume the mutaclysm test-harness's own script-vs-manual
   key-injection discrepancy (task #27, deliberately deferred all
   session).

---

## ❓ User question, asked at session close

**Q1: Is the save/load work (`rtp_manager.c`) hooked up to the new
Save/Open buttons in `editor_home.chtpm`?**

**No.** The real save/load system only exists behind `game.chtpm`'s
own "Save / Load" button, which links to `project_menu.chtpm`
(`rtp_manager.c`'s real screen — unchanged, still fully working).
`editor_home.chtpm`'s own toolbar "Save"/"Open" buttons send inert
`KEY:3001`-style codes that nothing handles — real, focusable, but
functionally dead placeholders, exactly as flagged in §10/the design
doc's own placeholder table. Wiring them to `rtp_manager.c` for real
(instead of routing through a separate Save/Load screen) is real,
not-yet-scoped follow-up work.

**Q2: Is the MAPS/GLYPHS picker on `editor_home.chtpm` real and
interactive like slop-ed-dev's own, or not yet?**

**Not yet.** What's on screen today is a static `<row>` showing real
data (the actual map directory names under `pieces/world_01/`, the
actual glyph set from `terrain_types.txt`) — but it's plain text, not
slop-ed-dev's real `<scroller>` widget. Nothing is focusable, nothing
switches the active map, nothing picks a glyph for placement. Building
that for real needs: (a) wiring the list through `<scroller>` instead
of `<row>`, and (b) actual map-switch / glyph-select backend logic
that doesn't exist yet (ties to the still-placeholder "Map Mode"
toolbar button). Skipped this pass specifically to avoid **PITFALL
68** (a custom dynamic variable freezing after first render) without
a real backend to drive it yet — real data, honest display, not yet a
real picker.
