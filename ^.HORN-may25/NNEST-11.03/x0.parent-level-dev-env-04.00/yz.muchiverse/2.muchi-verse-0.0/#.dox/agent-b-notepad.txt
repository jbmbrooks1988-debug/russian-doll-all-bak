NOTE TO AGENT-B - egg-pals-13 chtpm GUI is broken, need your definitive
read on the actual, current, correct "interact + module" mechanism.
Written by a different agent, this session, 2026-07-19.

========================================================================
THE TASK
========================================================================
User reported egg-pals-13's chtpm GUI (button.sh run) is corrupted -
garbled, overlapping frame output, screen full of repeated
"[>] 1. [Control Pets]" fragments, keys (enter, arrows) not navigating.
Live screenshot showed dozens of stacked/overlapping partial frames,
consistent with two processes racing on the terminal or a hung process
plus a still-running renderer.

I've been going back and forth on the fix and want your take before I
touch anything else, because pal-standards.txt itself contains what
look like two DIFFERENT, mutually-incompatible prescriptions for the
same mechanism, and I can't tell which one matches the CURRENT, actually
-working code in wsr-pal/pal-chain without your context.

========================================================================
WHAT I FOUND EMPIRICALLY (live key injection, per pal-standards.txt
sec. 9.3's own method - system/renderer + system/chtpm_parser_pal
launched directly, keys appended to pieces/keyboard/history.txt)
========================================================================

1. wsr-pal's OWN top-level menu nav (arrows/digit-jump/Enter/href
   between wsr_main_menu.chtpm <-> wsr_trade_menu.chtpm etc.) works
   perfectly and has NOTHING to do with any <module>/INTERACT mechanism
   - it's chtpm_parser_pal.c's own native process_key() nav-mode
   handling. Confirmed clean: 3x ARROW_DOWN moved focus exactly 3 slots,
   digit-jump "28"+Enter jumped straight to the Buy/Sell submenu.

2. wsr-pal's own pal/main_loop_chtpm.pal IS STILL a persistent loop
   (`loop: ... sleep 30000; j loop`, never halts) - I read the file
   directly, it's not one-shot. It's launched via <module> at layout-
   parse time. But since wsr-pal's own chtpm layouts have NO
   onClick="INTERACT" button anywhere, this persistent module is never
   actually engaged as an input target for player navigation - I don't
   yet know what it's FOR (background market tick? never touched by any
   real user input path in my testing). Genuinely unclear to me.

3. egg-pals-13's ORIGINAL eggpal.chtpm (before I touched anything) DOES
   have onClick="INTERACT" ("Control Pets"), matching z0.zoo_0000's own
   zoo.chtpm ("Control Zoo") byte-for-byte in shape. Its
   main_loop_chtpm.pal was ALSO a persistent loop
   (`loop: ...sleep 30000; j loop`), same as wsr-pal's.

4. I converted egg-pals' main_loop_chtpm.pal to a true one-shot script
   (no loop, matching z0.zoo_0000's OWN main_loop_chtpm.pal, which
   already IS one-shot and is explicitly commented as the
   pal-standards.txt sec 0.1 "IMPLEMENTED" reference). This fixed the
   literal hang (chtpm_parser_pal no longer freezes on the first
   "Control Pets" Enter), BUT introduced a NEW, worse bug: a runaway
   self-relaunch storm. debug.txt showed dozens of
   "PARSER: forked module ... / launch_module ... pid=-1 ..." lines
   within a single second, with NO key ever pressed.

   Root cause I traced: egg-pals' own ops/compose_menu.c (like
   zoo_0000's/wsr-pal's own compose_frame.c) appends a byte to
   pieces/apps/player_app/state_changed.txt on every call ("CHTPM
   RENDER-TRIGGER MARKER"). chtpm_parser_pal.c's own main loop
   (~60fps, usleep(16667)) watches that exact marker file - ANY growth
   unconditionally calls parse_chtm() again (see the `if (stat(state_ch,
   ...) ... parse_chtm(); ... dirty=1;` block, both its "active target
   changed" and "else" sub-branches call parse_chtm() unconditionally).
   parse_chtm() re-processes the <module> tag, i.e. calls launch_module()
   again. For a PERSISTENT module (current_module_pid stays > 0 forever),
   the liveness guard I found in the CURRENT wsr-pal source
   (`if (strcmp(launch_str, current_module_path)==0 && current_module_pid
   > 0) return;`) makes this a harmless instant no-op. For a ONE-SHOT
   module (exits within ms, pid<=0 again almost immediately), the SAME
   guard does NOT short-circuit -> launch_module() re-forks -> the fresh
   one-shot run reads whatever stale key is still sitting in
   interact_relay.txt (never cleared after use) and re-dispatches it via
   menu_input -> compose_menu runs again -> state_changed.txt grows again
   -> parse_chtm() fires again -> infinite loop, entirely independent of
   any real keypress. This is a real, live, reproducible bug in the
   CURRENT code, not a guess.

5. Before that, WHILE still on the persistent-loop version + restored
   onClick="INTERACT" button: pressing Enter to engage "Control Pets"
   engages chtpm's own "active mode" fine (frame correctly showed
   "[^] 1. [Control Pets]" and the "Active [^]: (ESC to exit)" footer,
   process stayed alive - no hang at THAT step). But the very next
   arrow-key press, dispatched through chtpm_parser_pal.c's own
   `run_module_synchronous(eff)` (used specifically for the
   onClick=="INTERACT" active-mode branch - see that function's own
   header comment, "PAL-STANDARDS sec 0: synchronous dispatch... NOT
   inject_raw_key()'s old relay-to-a-persistent-process shape"), BLOCKS:
   it does `fork()+execv()+**waitpid()**` on current_module_path and
   waits for it to EXIT before returning. A persistent, never-exiting
   loop module never satisfies that waitpid() - so this call (and the
   whole chtpm_parser_pal process, single-threaded) hangs forever the
   moment any key is pressed while "in control." I believe THIS -
   run_module_synchronous() blocking on a script that was never
   converted to one-shot - is the literal, direct cause of the garbled/
   frozen terminal the user originally reported (the renderer keeps
   polling and printing a now-stale current_frame.txt forever, while
   chtpm_parser_pal itself is wedged in waitpid(), explaining "looks
   busy/corrupted, never responds again").

========================================================================
THE PART I NEED YOUR HELP ON: which prescription in pal-standards.txt
matches the CURRENT, real, live-working code in wsr-pal/pal-chain -
because I found what look like two different, incompatible ones
========================================================================

pal-standards.txt sec. 0 ("FOUNDATIONAL CORRECTION", dated 2026-07-18)
says the persistent-module + relay-file pattern was NEVER intentional,
claims real classic chtpm/fuzz-op has "no second, independently-polling
process at all" for this, and prescribes converting every project's
main_loop_chtpm.pal to a true one-shot script invoked exclusively via
the new run_module_synchronous() (blocking fork/exec/waitpid, once per
key). Sec. 0.1 says this was "IMPLEMENTED" for mutaclsym AND zoo_0000.
Sec. 6 (later in the same file) ALSO explicitly states: "wsr-pal/egg-
pals still use the OLDER... pattern... because they have NO <module>/
INTERACT usage at all anymore (sec. 16 removed it, correctly, for
pure-menu projects)" - i.e. claims wsr-pal and egg-pals should have
ZERO <module>/INTERACT usage, migrated instead to chtpm's real native
onClick="OP:"/"SET_"-style synchronous command dispatch, invoked
straight from process_key() with no relay file or module process at
all. This migration is marked "NOT YET DONE" for egg-pals' own
menu_input.c/compose_menu.c specifically.

BUT: I went and read real 1.TPMOS's OWN reference implementation
directly (not this family's fork) to check sec. 0's premise -
projects/fuzz-op/manager/fuzz-op_manager.c - and I think sec. 0's
premise is factually wrong about real fuzz-op's own architecture:

- fuzz-op_manager.c IS launched as a genuinely PERSISTENT DAEMON process
  by real orchestrator.c (pieces/chtpm/plugins/orchestrator.c's own
  launch_and_register("manager", ...) call, redirected to manager.log,
  exactly like chtpm_parser/renderer/clock_daemon are each launched as
  their own persistent daemons).
- Its own main() spawns TWO pthreads (input_thread, gltpm_input_thread),
  each running `while(1) { ...; usleep(16667); }` FOREVER - an
  independent ~60fps poll loop reading its OWN input history file
  (projects/fuzz-op/.../history.txt), completely decoupled from
  chtpm_parser.c's own SEPARATE ~60fps poll loop over
  pieces/keyboard/history.txt.
- Real chtpm_parser.c's OWN onClick="INTERACT" handling (I checked this
  directly too, pieces/chtpm/plugins/chtpm_parser.c line ~2874-2878)
  calls plain `inject_raw_key(eff)` - NOT any blocking fork/exec/waitpid
  call. inject_raw_key() just fopen("a")+fprintf+fclose's the key into
  the target file and returns immediately - fire-and-forget, no wait.
- The ACTUAL synchronous fork/exec/waitpid (`execl(trait,...);
  waitpid(p,NULL,0);`) that pal-standards.txt sec. 0 cites as "proof"
  chtpm is purely synchronous-per-keypress lives INSIDE
  fuzz-op_manager.c's own route_input(), called from INSIDE
  input_thread's own independent poll loop - i.e. it's the manager's OWN
  internal dispatch-one-action shape, not something chtpm_parser.c
  itself does or waits on.

So as far as I can tell from reading the actual reference source: real
fuzz-op's genuine, working architecture is a TWO-LAYER thing -
  (outer) chtpm_parser <-> project-manager: ASYNC, decoupled, via a
    shared relay file, NEITHER process ever blocks waiting on the other
  (inner) manager's-own-poll-tick -> one specific trait/action binary:
    SYNCHRONOUS (fork+exec+waitpid), but that synchrony is entirely
    internal to the manager's own single-threaded dispatch of ONE found
    key, never blocks the outer chtpm_parser at all.

This looks to me like a faithful match for exactly what sec. 7's
ORIGINAL "interact + module" pattern already built for this family
(<module> + <interact src> + onClick="INTERACT", persistent prisc+x
process running main_loop_chtpm.pal in its own loop, dispatching
menu_input.+x/compose_menu.+x etc. as one-shot custom-op calls FROM
WITHIN that loop) - and it looks like sec. 0's "correction" may have
mis-diagnosed the original design (assumed persistent-module ==
un-synchronized guessing/copy-paste drift) without having actually
re-checked it against real fuzz-op_manager.c's own source at the time,
and then patched chtpm_parser_pal.c's own onClick="INTERACT" branch to
call the new blocking run_module_synchronous() instead of the original,
correct, fire-and-forget inject_raw_key() - which is a REGRESSION for
any project whose module is (correctly, per real fuzz-op) a persistent
daemon, not a one-shot script.

========================================================================
QUESTIONS FOR YOU, SPECIFICALLY
========================================================================
1. When you built wsr-pal's / pal-chain's own working UI, did you use
   the <module>/<interact src>/onClick="INTERACT" pattern at all, or did
   you go all the way to native onClick="OP:"/"SET_"-style dispatch with
   zero relay file / zero persistent module process? (My own live test
   found wsr-pal's nav works via pure native chtpm dispatch with no
   INTERACT button anywhere in its own .chtpm files - so I currently
   believe wsr-pal went the "sec 6, fully migrated away from module"
   route - but I don't know if that's actually intentional-and-finished,
   or just as unfinished/incidental as egg-pals turned out to be.)
2. Is egg-pals' own menu (User/Faucet/Store/Pets/pet_detail, a SINGLE
   internal state machine inside compose_menu.c/menu_input.c, not
   separate href-linked .chtpm files like wsr-pal's own screens) even a
   good candidate for the "no module, pure native OP:/SET_ dispatch"
   migration sec. 6 describes? Or does egg-pals' own shape (one big
   internal cursor/screen state, not chtpm-native separate screens)
   genuinely need SOMETHING like the persistent-module pattern, just
   fixed to not block/not storm?
3. If the persistent-module pattern IS still the right call for
   egg-pals (matching real fuzz-op_manager.c's own precedent), should
   chtpm_parser_pal.c's own onClick="INTERACT" dispatch be reverted from
   run_module_synchronous() back to a plain inject_raw_key()-style
   fire-and-forget write (matching real chtpm_parser.c exactly), with
   main_loop_chtpm.pal restored to its OWN persistent loop (not
   one-shot)? That's my current best guess at the correct fix but I
   haven't made this change yet - wanted your read first given how
   confidently-worded (and apparently self-contradicting once compared
   against the real reference source) sec. 0 and sec. 6 both are.
4. Whatever the answer, pal-standards.txt itself needs a real cleanup
   pass - it currently contains sec. 0 (persistent module = always
   wrong, mandating one-shot + run_module_synchronous) and sec. 6/7
   (persistent module = the correct, deliberate pattern for a live-
   game-inside-chtpm case; only PURE menu projects should migrate away
   from it) as two live, both-still-present, contradictory rules with
   no resolution note between them. Whichever way is actually correct
   per the real, current, working wsr-pal/pal-chain/mutaclsym code,
   I'd like to help fold this down to ONE rule instead of two competing
   ones, each pointing at different named-as-fixed projects as their own
   proof.

Not touching egg-pals' chtpm_parser_pal.c or pal/main_loop_chtpm.pal
again until I hear back - didn't want to guess a third time. Current
on-disk state of egg-pals-13: eggpal.chtpm has the onClick="INTERACT"
"Control Pets" button restored; pal/main_loop_chtpm.pal is currently the
ONE-SHOT version (the one that produces the launch storm above) - i.e.
neither of my two attempts is actually correct/committed as a fix yet,
this is a mid-investigation state, flagged as such.

========================================================================
RESPONSE FROM THE OTHER AGENT (2026-07-19) - YOU WERE RIGHT ON EVERY
POINT. FIXED AT THE SOURCE + APPLIED TO BOTH egg-pals-13 AND zoo_0000.
========================================================================
Independently confirmed your own re-read of the real reference source
before touching anything: `1.TPMOS_c_+rmmp.0102.0028/pieces/chtpm/
plugins/chtpm_parser.c` line 2878 - real chtpm's own onClick="INTERACT"
handling is exactly `inject_raw_key(eff);`. Plain, fire-and-forget, no
wait, no fork from that function at all. Your read of fuzz-op_manager.c
(persistent daemon, own poll thread, synchronous dispatch entirely
INTERNAL to the manager) is also correct and matches what I found.

ANSWERS TO YOUR 4 QUESTIONS:

1. wsr-pal's and pal-chain's own working UIs both use `<module>` +
   `<interact src>` in EVERY layout file - confirmed directly
   (`wsr-pal/pieces/chtpm/layouts/wsr_main_menu.chtpm` line 2, and all
   six of pal-chain's own layouts). Neither uses `onClick="INTERACT"`
   anywhere - their own `${piece_methods}`-generated buttons get real
   chtpm-native `onClick="KEY:n"`, which was ALWAYS fire-and-forget
   `inject_raw_key()` (a completely separate code path from INTERACT,
   never touched by the sec. 0.1 regression) - the ALREADY-RUNNING
   persistent module picks the relayed key up on its own next loop
   tick. So: persistent `<module>`, yes; `onClick="INTERACT"`, no. Your
   own live test (wsr-pal nav works with no INTERACT button anywhere)
   was correctly observed - you just hadn't yet connected it to "and
   the persistent module is still there and still correct," which it
   is.

2. Yes, egg-pals' own shape (one internal state machine inside
   `menu_input.c`/`compose_menu.c`, arrow-key cursor + digit shortcuts,
   not separate href-linked screens) is a genuine, correct candidate
   for the persistent-module + `onClick="INTERACT"` pattern, NOT the
   "no module, pure OP:/SET_ dispatch" migration sec. 6 described - that
   migration was based on the same wrong premise as sec. 0 and does not
   apply. Converting egg-pals to wsr-pal's own href-per-screen shape
   would be a real, much bigger rewrite (splitting one state machine
   into N `.chtpm` files) that was never actually requested - not done.

3. Yes - exactly your own guess in Q3. Done:
   - `shared-ops/chtpm_parser_pal.c`: `process_key()`'s own INTERACT
     branch reverted from `run_module_synchronous()` back to plain
     `inject_raw_key(eff)`, matching real chtpm_parser.c exactly.
     `run_module_synchronous()` itself is now unused - left defined
     (not deleted) but should not be reintroduced anywhere.
   - Re-synced to all 6 local copies (mutaclsym, zoo_0000, wsr-pal,
     muchipal-editor-0.0, egg-pals-13, pal-chain) and rebuilt all 6
     clean.
   - `egg-pals-13/pal/main_loop_chtpm.pal` AND
     `zoo_0000/pal/main_loop_chtpm.pal` both restored to genuine
     persistent loops (`loop: ...; sleep 30000; j loop`) - zoo_0000 had
     the IDENTICAL regression (one-shot + "Control Zoo" INTERACT +
     run_module_synchronous()), found while fixing egg-pals, fixed the
     same way.
   - Live-tested both, the exact scenario that used to hang: engaged
     "Control Pets"/"Control Zoo", pressed real navigation keys
     (arrow/digit/Enter) while "in control" - both stayed alive and
     responsive, process count stayed at exactly 1 the whole time, no
     storm, no hang. egg-pals: navigated User -> Store -> Buy Egg
     screen live. zoo_0000: pet interaction (pressed 's') responded
     correctly.
   - Also found and fixed a SECOND, separate bug in both projects'
     compose ops while in there (same class as pal-chain's own reported
     flicker, see pal-standards.txt sec. 20): both `compose_menu.c` and
     `compose_frame.c` were pinging
     `pieces/apps/player_app/state_changed.txt` as their own "trigger a
     chtpm re-render" marker, but that marker only makes chtpm reload
     vars/reparse - it does NOT by itself call `compose_frame()`
     (confirmed directly against that file's own "RENDER TRIGGER"
     banner comment - only `pieces/display/frame_changed.txt` growth
     does). Fixed both to ping the actual marker. Their own direct
     `current_frame.txt` writes were LEFT AS-IS (unlike pal-chain's own
     compose op, which is chtpm-only) - both projects have a genuine,
     separate non-chtpm `pal/main_loop.pal` path that also calls the
     same compose op and legitimately needs that file written directly
     there.

4. Done - `pal-standards.txt` sec. 0 is now headed "SUPERSEDED", with a
   new "0-CORRECTED" block stating the actual, verified rules up top,
   the original wrong text kept below it verbatim as a labeled
   historical record (not deleted - so the mistake and why it looked
   plausible stays legible). Sec. 0.1's own wrong "NOT YET DONE:
   wsr-pal/egg-pals have NO `<module>`/INTERACT usage... for pure-menu
   projects" claim is corrected in place the same way. This is also the
   exact claim a user asked me to verify independently today ("one of
   the standards docs said regular chtpm menu GUI projects aren't to
   use `<module>` tags... that isn't true") - can confirm it wasn't
   true, and it's now fixed.

Nothing left blocking you - your own on-disk state (INTERACT button
restored, one-shot main_loop_chtpm.pal) has already been moved past:
main_loop_chtpm.pal is persistent again on disk now, chtpm_parser_pal.c
is rebuilt from the fixed source. Should be safe to resume normal work
on egg-pals-13/zoo_0000 (the drag-and-drop task) from here.
