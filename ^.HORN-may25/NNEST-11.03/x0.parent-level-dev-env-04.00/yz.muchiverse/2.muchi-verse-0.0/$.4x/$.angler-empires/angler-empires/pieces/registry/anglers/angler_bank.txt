# angler bank - TYPE/TEMPLATE registry, one row per angler role that can
# exist. Per ANGLER-EXPANSES-DESIGN.txt sec. 3 (bank vs instance-dir
# split, zest-er-summary sec. 4.4): this file is static content, never
# mutated at runtime. A LIVING angler (spawned into a specific world's
# map_start/anglers/<instance_id>/state.txt) copies its own starting
# fields FROM one of these rows at spawn time (ops/spawn_angler.c) and
# then owns its own separate, mutable state from then on - this file is
# never re-read for an already-spawned instance.
#
# CORRECTED 2026-07-21 (direct user catch, "the player doesn't control
# any of the anglers, they just use a picker cursor"): this is Dwarf
# Fortress, not the mansion-sim's own is_player=1 hero-you-move model.
# ALL 7 home-tribe anglers are AI-driven - is_player is kept as a
# column (every row currently 0) only because spawn_angler.c/state.txt
# already reads it and a future non-hostile "possess" feature could
# conceivably use it, NOT because any row is player-moved today. The
# player instead drives a free-roaming CURSOR piece (real, proven prior
# art: 1.TPMOS_c_+rmmp.0102.0028/projects/fuzz-op/pieces/xlector -
# type=xlector, methods scan/collect/place/inventory issued AT the
# cursor's own position, not tied to a controllable character). This
# project's own hero/state.txt ALREADY has the matching mechanism
# (xlector_pos_x/xlector_pos_y + interact_mode, dox 04's "interact
# mode," confirmed working before this session) - the fix is to lean on
# that existing cursor for designation/selection (angler-expanses
# design doc sec. 4's job system), not to make any bank row directly
# player-moved.
#
# Format: id|name|glyph|color|is_player|role|default_outfit|tribe_id
#   is_player: always 0 in this file now (see correction above).
#   role: miner|builder|forager|defender|crafter|scholar|leader (labor-
#     role, matched against job_types.txt's own requires_role column
#     once that registry exists - see design doc sec. 4, not built yet).
#   default_outfit: reused column, same meaning as old characters.txt.
#   tribe_id: which of the 7 tribes this row belongs to - tribe_angler_home
#     is the player's own starting tribe; the 6 enemy tribes get their
#     own 7-row blocks appended here later (design doc sec. 5), not
#     built yet - this file currently has ONLY the home tribe's 7 rows.
#
# Names below are a first pass, not final - confirm with user before
# treating as permanent flavor text.
ang_leader|Finn|@|magenta|0|leader|outfit_none|tribe_angler_home
ang_miner|Grubb|m|yellow|0|miner|outfit_none|tribe_angler_home
ang_builder|Tox|b|cyan|0|builder|outfit_none|tribe_angler_home
ang_forager|Reel|f|green|0|forager|outfit_none|tribe_angler_home
ang_defender|Spike|d|red|0|defender|outfit_none|tribe_angler_home
ang_crafter|Weave|c|white|0|crafter|outfit_none|tribe_angler_home
ang_scholar|Sage|s|blue|0|scholar|outfit_none|tribe_angler_home
