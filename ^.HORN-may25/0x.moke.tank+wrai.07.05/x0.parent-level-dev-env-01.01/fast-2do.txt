# Wraith Fast 2Do

Last updated: 2026-06-27

## Current Baseline

Wraith has moved past the immediate GL parity repair sprint.

Completed enough to proceed:
- GL presenter is thin and displays `projects/wraith-alpha/session/rgb/current_frame.rgba32`.
- RGB conversion is owned by `wraith_rgb_daemon`.
- ASCII/GL mode is session-controlled.
- stale previous-session GL frames are cleared.
- GL keyboard supports 10+ nav indices.
- GL mouse coordinates are transformed through the displayed texture viewport.
- RGB and GL receipts now expose enough data to debug display parity:
  - `projects/wraith-alpha/session/rgb/current_frame.receipt.pdl`
  - `projects/wraith-alpha/session/rgb/gl_display.receipt.pdl`
  - `projects/wraith-alpha/session/rgb/gl_input.receipt.pdl`
- semantic text projection now uses the same cell metrics as panels and receipts.
- `wraith-3d-cube`, `piececraft-wraith`, and `chtmgl-wraith` now all exist as internal Wraith validation project tracks.
- `emoji-studio-wraith` now exists as the fourth internal Wraith validation project track for RGBA/image extrusion inside `${game_map}`.
- the Wraith manager now emits specialized non-terminal project body text/semantic objects for these probes instead of showing only the launcher list inside every project window.
- user confirmed Step 1 live: project-owned `session/wraith_body.txt` and `session/scene.objects.pdl` bridge into Wraith Alpha without project-specific manager branches.
- generic `INTERACT` bridge is now required for `${game_map}` surfaces: `[>]` selected, Enter enters map control, active selector becomes `[^]`, `ESC` exits, and keypresses are appended to project `session/history.txt`.
- first-pass RGB converter previews exist for `zslice_piece`, `tile_zmap`, and `rgba_extrusion`.
- focused 3D/widget conversion plan lives in `wraith-chtmgl&3d.md`.

Do not restart by tweaking footer/chrome visuals unless a fresh receipt proves a regression.

## Momentum Assessment

Current ranking:

1. `piececraft-wraith` has the strongest momentum.

Reason:
- source reference `piececraft-3d` already uses `${game_map}`, `INTERACT`, and `is_map_control`.
- source reference also has the camera/POV contract Wraith needs:
  - `camera_mode`
  - `cam_x/cam_y/cam_z`
  - `cam_pitch/cam_yaw/cam_roll`
  - `CAMERA_SET`
  - `CAMERA_MODE`
  - `CAMERA_MOVE`
- it has a real z-map and tile vocabulary.
- the Wraith version now has a starter map slice and state file.

2. `wraith-3d-cube` is the smallest and safest first verification.

Reason:
- it is intentionally narrow.
- it now has visible cube audit body content.
- it should be used to prove the project-body/display/receipt path before making Piececraft deeper.

3. `emoji-studio-wraith` has clear reference momentum but needs a new RGB extrusion primitive.

Reason:
- source reference already has `voxels_<res>.csv` files.
- the conversion concept is clean: RGBA pixels become extruded colored cells.
- it should stay limited to `${game_map}` surfaces.

4. `chtmgl-wraith` needs the most widget converter work.

Reason:
- source reference `chtmgl-alpha` uses richer widgets and a `canvas` tag.
- Wraith is still standardizing on `${game_map}` for now.
- widget roles need first-class semantic/RGB primitives before it can be considered more than a placeholder audit page.

## First Rule

All new Wraith validation projects belong under:

`1.TPMOS_c_+rmmp.0102.0027/projects/wraith/wraith-projects/`

Do not create top-level TPMOS projects for these validation targets.

## Modularity Rule

Do not hardcode validation-project bodies, scene records, ops, cube data, map data, widget data, or emoji extrusion data in `wraith-alpha_manager.c`.

Current file-owned Wraith project bridge:
- `session/wraith_body.txt` controls the body/audit text shown inside the Wraith window.
- `session/scene.objects.pdl` controls project semantic objects imported into the frame.
- project assets remain under that project: `pieces/**`, `maps/**`, `assets/**`, `layouts/**`.

Wraith Alpha may discover and host projects. Individual Wraith projects must own their own layouts/state/scene/pieces/maps/assets. If it is not in a file or receipt, it is not project truth.

## Current Step 2/3 Priority

Immediate next implementation checks:
- Verify `wraith-3d-cube` has a project-owned `${game_map}` surface record with `nav=<n>` and `action=INTERACT`.
- Verify `piececraft-wraith` has the same `INTERACT` surface bridge.
- In both projects, Enter on that nav should switch `[>]` to `[^]`; `ESC` should switch it back.
- While `[^]` is active, keyboard input should append to that project's `session/history.txt` instead of driving Wraith shell nav.
- `session/history.txt` exists in all four validation projects; use it as the next project-manager input queue.
- Project-specific cube rotation, Piececraft camera movement, and map updates must be implemented in project-owned managers/ops, not in `wraith-alpha_manager.c`.

## Task 1: Finish `wraith-3d-cube`

Path:
- `1.TPMOS_c_+rmmp.0102.0027/projects/wraith/wraith-projects/wraith-3d-cube`

Goal:
- a small cube probe that opens from the Wraith internal launcher.
- ASCII shows an auditable cube/project body.
- GL shows a graphical-block/RGB-shape cube using a software cube rasterizer in the RGB converter.
- cube camera controls should follow Piececraft's camera command model.
- receipts identify the cube/map region and converted primitives.

Minimum implementation:
- one Wraith project window.
- one `${game_map}` cube surface.
- one cube piece artifact stored as 2D z-slices under `pieces/cube_probe/artifact.txt`, following the existing `piececraft-3d` / `fuzz-op-gl` piece convention.
- one static or stepped cube frame.
- one clear ASCII audit projection.
- one GL projection through the semantic RGB converter.

Acceptance:
- selecting `wraith-3d-cube` inside Wraith opens real project body content.
- no top-level project launch is required.
- GL and ASCII show the same project truth.
- `current_frame.receipt.pdl` and `gl_display.receipt.pdl` checksums agree.

Current status:
- project exists.
- layout has a cube audit body.
- session state exists.
- manager loads project body text from `session/wraith_body.txt`.
- manager loads project semantic scene hooks from `session/scene.objects.pdl`.
- scene declares `nav=5 action=INTERACT` for the cube `${game_map}` surface.
- first-pass `zslice_piece` preview exists in `wraith_rgb_daemon.c`, fed by `pieces/cube_probe/artifact.txt`.
- live check confirms the cube opens and the `[^]` interact marker can appear.
- `session/history.txt` records `COMMAND: INTERACT`.
- camera controls are not implemented yet; no cube project op consumes history or updates camera/rotation state.
- the current GL cube is a first-pass stacked-face preview, not the final camera-projected cube primitive.
- next implementation step is project-owned cube ops that consume `session/history.txt` and update camera/rotation state.
- next verification is a live launch from Wraith and receipt check.

Immediate cube subtasks:
- Add a project-owned cube manager/op under `wraith-3d-cube`, not inside `wraith-alpha_manager.c`.
- Consume `session/history.txt` while `is_map_control=1`.
- Translate keys into `CAMERA_MODE`, `CAMERA_SET`, `CAMERA_MOVE`, and `ROTATE_CUBE`.
- Update `session/state.txt` and `pieces/cube_probe/state.txt`.
- Regenerate `session/wraith_body.txt` so ASCII audit reflects changed rotation/camera.
- Regenerate or enrich `session/scene.objects.pdl` so RGB conversion receives camera/rotation metadata.
- Replace the preview renderer with a real projected cube path: load z slices, build occupied cells/faces, apply rotation/camera, depth-sort, draw shaded faces.

## Task 2: Create `piececraft-wraith`

Reference:
- `1.TPMOS_c_+rmmp.0102.0027/projects/piececraft-3d`

New Wraith project path:
- `1.TPMOS_c_+rmmp.0102.0027/projects/wraith/wraith-projects/piececraft-wraith`

Goal:
- validate Wraith against a z-level/tile/game-map project.
- prove `${game_map}` + `INTERACT` + `is_map_control` work with map depth and tile metadata.
- move toward the Piececraft GL-OS screenshot style using RGB-rendered camera-projected 3D blocks.
- use Piececraft's existing `maps/map_01_z*.txt` + `assets/tiles/registry.txt` + `assets/tiles/*.tile.txt` standard, not a new voxel-only world format.

Minimum implementation:
- do not clone the whole project first.
- start with a small z-level map slice.
- expose tile id, z-level, selected cell, and interaction state in ASCII audit.
- render a GL map surface through semantic/RGB conversion.

Acceptance:
- map region is visible in both ASCII and GL.
- `INTERACT` mode can be entered/exited without breaking desktop nav.
- receipts expose map bounds, active z-level, and selected/interacted cell.

Current status:
- project exists.
- starter layout, map slice, session state, and README exist.
- manager loads project body/scene records from project-owned session files.
- scene declares `nav=5 action=INTERACT` for the map `${game_map}` surface.
- first-pass `tile_zmap` preview exists in `wraith_rgb_daemon.c`, fed by `maps/map_01_z0.txt`.
- this is currently the best candidate to polish after cube launch is verified.
- next implementation step is a `voxel_tile`/`game_map` 3D block primitive in the RGB converter using camera mode/position/rotation.

## Task 3: Create `chtmgl-wraith`

Reference:
- `1.TPMOS_c_+rmmp.0102.0027/projects/chtmgl-alpha`

New Wraith project path:
- `1.TPMOS_c_+rmmp.0102.0027/projects/wraith/wraith-projects/chtmgl-wraith`

Goal:
- validate Wraith against markup-driven widgets and CHTMGL-like layout.
- prove markup primitives become Wraith semantic scene objects, then RGB/GL shapes.
- move from text placeholders to widget primitives: panels, buttons, checkbox, sliders, media boxes, menu, and `${game_map}` preview surface.

Minimum implementation:
- start with a small page, not the whole reference project.
- include representative controls:
  - window
  - header
  - panel
  - button
  - checkbox
  - slider
  - image/media placeholder
  - menu
  - `${game_map}` surface if canvas-like behavior is needed

Acceptance:
- controls are visible and indexable in ASCII and GL.
- GL renders shapes/widgets rather than only terminal glyphs.
- receipts expose roles, labels, bounds, hit regions, focus regions, and actions.

Current status:
- project exists.
- starter layout, session state, and README exist.
- manager loads project body/scene records from project-owned session files.
- this needs the most future work: real widget semantic primitives and RGB conversion beyond text placeholders.

## Task 4: Create `emoji-studio-wraith`

Reference:
- `#.emoji-studio-501.02.05t/&.emoji-studio-solo.02.01`

New Wraith project path:
- `1.TPMOS_c_+rmmp.0102.0027/projects/wraith/wraith-projects/emoji-studio-wraith`

Goal:
- validate Wraith against 2D RGBA/image-to-3D extrusion inside `${game_map}`.
- preserve Emoji Studio's existing `pieces/<emoji_name>/voxels_<res>.csv` convention.
- prove image-like content can become RGB-rendered 3D cells without changing normal Wraith UI rendering.

Minimum implementation:
- sample `pieces/sample_emoji/voxels_8.csv`.
- ASCII audit body with source path, resolution, and conversion policy.
- semantic `IMAGE_EXTRUSION role=rgba_extrusion` object.
- RGB converter primitive that extrudes alpha-visible pixels into camera-projected colored cells.

Acceptance:
- launcher lists `emoji-studio-wraith`.
- selecting it opens a specialized Wraith project body.
- GL receipt identifies source CSV, resolution, visible pixel count, projected bounds, and checksum.
- normal desktop/window/widget rendering remains unaffected.

Current status:
- project exists.
- starter layout, sample CSV, session state, README exist.
- manager loads project body/scene records from project-owned session files.
- scene declares `nav=5 action=INTERACT` for the extrusion `${game_map}` surface.
- first-pass `rgba_extrusion` preview exists in `wraith_rgb_daemon.c`, fed by `pieces/sample_emoji/voxels_8.csv`.
- next implementation step is a receipt-rich extrusion primitive with visible pixel counts/projected bounds and project-owned edit ops.

## Required Audit Workflow

Before changing visual layout:
1. Read `projects/wraith-alpha/session/rgb/current_frame.receipt.pdl`.
2. Read `projects/wraith-alpha/session/rgb/gl_display.receipt.pdl`.
3. Confirm `render_checksum_fnv1a64` matches `loaded_rgba_checksum_fnv1a64`.
4. Inspect object `text_px_*`, `hit_px_*`, and `focus_px_*`.
5. Only then change coordinates, projection, or styling.

Before changing mouse behavior:
1. Read `projects/wraith-alpha/session/rgb/gl_input.receipt.pdl`.
2. Check raw mouse, texture viewport, mapped texture coordinates, and `inside_texture`.
3. Do not change ASCII mouse offsets to fix GL fullscreen issues.

## Architecture Reminders

- One scene, two presenters.
- ASCII is an audit/debug projection.
- GL is a graphical presentation projection.
- `wraith_gl.c` should stay thin.
- `wraith_rgb_daemon.c` should own RGB conversion.
- the manager/session artifacts should own Wraith window/project truth.
- `${game_map}`, `INTERACT`, and `is_map_control` are the uniform names for now.
- `piececraft-wraith` and `chtmgl-wraith` are new Wraith project names; do not silently reuse source project names unless the code/project is truly the same.
- 2D-to-3D conversion is special to `${game_map}` surfaces only:
  - cube/entity: `pieces/<id>/artifact.txt` z-slice piece.
  - world/map: `maps/map_01_z*.txt` plus tile registry/extrude.
  - image/emoji: `pieces/<name>/voxels_<res>.csv` RGBA extrusion.
