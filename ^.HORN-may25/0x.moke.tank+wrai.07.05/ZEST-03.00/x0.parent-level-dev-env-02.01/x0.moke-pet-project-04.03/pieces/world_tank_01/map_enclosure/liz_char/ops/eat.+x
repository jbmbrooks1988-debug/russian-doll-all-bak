#!/bin/bash
FOOD_DIR="../food_pool"
STOMACH_DIR="../stomach"
FOOD=$(ls "$FOOD_DIR" | head -n 1)
if [ -n "$FOOD" ]; then
    mv "$FOOD_DIR/$FOOD" "$STOMACH_DIR/"
    echo "Ate $FOOD"
else
    echo "No food found"
fi
