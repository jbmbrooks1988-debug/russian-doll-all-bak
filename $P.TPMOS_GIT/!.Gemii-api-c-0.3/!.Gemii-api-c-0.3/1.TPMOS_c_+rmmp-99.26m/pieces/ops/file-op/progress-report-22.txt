# Progress Report 22: File-Op Globalization Fix
**Date:** 2026-05-22
**Status:** IMPLEMENTING UNIFIED THEATER

## 1. What was done
- **Created Global Infrastructure:** Established `pieces/ops/file-op/` with `src/`, `+x/`, and `layouts/`.
- **Implemented Reusable Muscle:** Created `file_op.+x` (atomic moves), `sync_op.+x` (state updates), and `project_file_op.+x` (proven slot logic).
- **Ported Advanced UI:** Moved the `agy`-style directory browser and file menu to the global layouts folder.
- **Unified Manager:** Implemented `file_manager.c` to handle the logic for these shared layouts.

## 2. The Hiccup (Why it felt broken)
- **Empty Directories:** The global manager was failing to find files due to incorrect `project_root` resolution and persistent search queries.
- **Process Competition:** Project managers were fighting with the global manager for keyboard input.
- **State Clobbering:** The global manager was occasionally wiping critical state needed by the projects to render their main views.

## 3. The Plan (Current Action)
- **Shared Stage:** Point option 8 in `op-ed` and option 4 in `agy` to the *same* global layouts: `pieces/ops/file-op/layouts/file_menu.chtpm`.
- **Delete Garbage:** Remove the old, project-specific file menus in `op-ed`.
- **Robust Manager:** Finalize `file_manager.c` to:
    - Correctly display files and folders for ANY project dir.
    - Support both "Load Project" (entire dir copy) and "Load File" (buffer import).
    - Use `${return_layout}` to ensure users always get back to where they started.
- **Re-Compile Everything:** Ensure all binaries are synced with this unified vision.

## 4. Final Handoff State (m22)
- **Integration Successful:** Both `op-ed` and `agy` now use `pieces/ops/file-op/layouts/file_menu.chtpm`.
- **Cleanup Complete:** Deleted 6 redundant layout files from project directories.
- **Critical Bug:** The system is "stuck" in the File Menu. Navigation works, but selecting options (like "LOAD FILE...") doesn't transition to the Browser layout.
- **Probable Cause:** `file_manager.c` might not be detecting the `KEY_PRESSED:` codes correctly or failing to update `current_layout` via `layout_changed.txt`.
- **Next Steps:**
    1. Debug `pieces/ops/file-op/src/file_manager.c`. Ensure it handles keys 6-9 by writing the new layout path to `layout_changed.txt`.
    2. Verify `update_gui_state()` is writing to the correct path that the parser is watching.
    3. Ensure `project_id` and `return_layout` are correctly set in the shared state file before the manager starts.

*"The plumbing is global, but the faucet is still dry. Prime the manager."*

