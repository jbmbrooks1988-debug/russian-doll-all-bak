@\!.gem-flashlite--yolo/\!.evo\&obs/\!.TPMOS_ONBORD_BIBLE_2.md is our    
   project, currently we are working on the op-ed project . which will be a sort of ascii version of "rpg maker mz"
   
  before we start on anything , inside if op-ed/docs/oped-report-m23.txt 
   
   i want a report of our current progress with the following : 
   
   1. file-op setup. currently , we want it to beable to load "games" that is individual dirs within the games/ dir
   each one of a unique project name, including default and test game. 
   to maintain flexibility we should reference the functionality of agy-text-editor 
   (which  works nearly perfectly but has a problem loading text files, which u can also address as a sub topic , 
   just to keep us grounded and give context)
   and make sure we can use file-op 's without breaking its ability to be used in other projects other ways.
   
   we already have a "game local" map creation and loading mechanic in the op-ed navigator under maps, 
   which we should reuse ops from 
   
   1 problem i notice maps and the current example project fuzz-op mirror (which is meant to mirror fuzz op but 
   run with pal script macro manager inside of the op-ed player (not done yet)
   
   are erroneously outside the games/ dir.(u should recognize this sort of scaffolding rough draft that needs fixing and suggest next step solutions) this dir is where the individual games projects will be saved and loaded from .
   some macro infrastructure for this unique setup may not yet be in space yet.
   
   2.game loading  , sa@ving and playing is different from agy-text-editors use of saving and loading
   
   when we save a game it should have a 'macro prisc player script and data files'
   
   we need to outline this macro structure it doesn't exist yet. also player doesn't exist yet
   
   3.player will play the game that is loaded into editor
   (the player will also be extracted and shipped on its own if game is "exported" (along with w/e else kbd/io other tpmos stuff
   so make sure its modular and can stand alone , this is a big project i know, but dont cut corners, thats why were planning it now)
   explain exactly what will have to be exported how player will work , locally and as export , reusing ops
   whenever possible etc.
   4. we will have a test project . maybe we should build player first? 
   
   player should beable to run full games when exported
   but im not sure exactly how this should be done
   should it be a big pal script that calls ops
(there wont be "project managers" like in the tpmos project, because they are playing custom user made games, get it?)

5. pal op editor? when events are clicked we are supposed to beable to edit their "local pal script"
attached to their pieces (obvious there are also 'global-events' that user can edit 
for the entire 'game' dir , which will also run during exported script)
   
   investigate the codebase , explain our progress and future todo's and the most logical phases to do them. 
   i want to make sure we are on the same page before we start.
   
   as u can see this is very confusing and suggests many circular dependances
   so be thorough and help outline the exact developer roadmap. i will read over what u write and make sure it makes sense
   before we do anything
